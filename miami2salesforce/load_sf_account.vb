Public Class load_sf_account
    '    Inherits ModeleExtraction

    '#Region "Constructeur"

    '    Public Sub New(ByVal pPilote As IPiloteExtraction)
    '        MyBase.New(pPilote)
    '    End Sub

    '#End Region

    '    Public Overrides Sub Generer()
    '        Pilote.EcrireMessage(" === DEBUT TRAITEMENT === ", CstExtraction.TypeMessage.DebutTraitement)

    '        Try
    '            Extraire()
    '        Catch ex As Exception
    '            Pilote.EcrireMessage(ex.Message, CstExtraction.TypeMessage.Erreur)
    '        End Try

    '        Pilote.EcrireMessage(" === FIN TRAITEMENT === ", CstExtraction.TypeMessage.FinTraitement)
    '    End Sub

    '    Public Overrides Sub Extraire()
    '        'mon traitement...
    '    End Sub

End Class
